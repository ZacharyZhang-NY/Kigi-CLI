{
  "name": "kigi-frontend",
  "version": "0.0.0",
  "private": true,
  "description": "kigicli.dev — static marketing site plus the installer proxy Worker.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
